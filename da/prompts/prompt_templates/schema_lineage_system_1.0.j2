As a SQL expert for {{ dialect }} databases, analyze the user's question and database schema to:

1. Find most relevant tables using:
   - Keyword match (table/column names)
   - Table relationships (foreign keys)
   - Time range coverage
2. Explain why other tables are excluded
3. Output JSON with:
   - Matched tables (sorted by relevance) with full table name: 

Required input:
- Business question
- Database DDL

Example output:

```json
[
  {
    "table": "schema1.orders",
    "score": 0.8,
    "reasons": ["has 'order_date' field", "links to customers"]
  }
]//...
``` 